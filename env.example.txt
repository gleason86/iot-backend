# IoT Backend Environment Configuration
# Copy to .env and fill in values (scripts/setup.ps1 generates one for you).

# =============================================================================
# MQTT (Mosquitto)
# =============================================================================
MQTT_USER=mqtt_user
MQTT_PASSWORD=change_this_password

# =============================================================================
# InfluxDB
# =============================================================================
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=change_this_password
INFLUXDB_ORG=home
INFLUXDB_BUCKET=iot
INFLUXDB_ADMIN_TOKEN=change_this_to_a_secure_token
# Write-only token for Telegraf. Create after first start (operator token stays in the container):
#   docker exec iot-influxdb sh -c 'influx auth create -o home -t "$DOCKER_INFLUXDB_INIT_ADMIN_TOKEN" --write-bucket <iot bucket id> --description "telegraf write-only, iot bucket"'
INFLUXDB_TELEGRAF_TOKEN=change_this_to_a_write_only_token

# Grafana lives in ~/Repos/grafana (its own .env holds the read-only token and admin password).
