# IoT Backend Environment Configuration
# Copy to .env and fill in values (scripts/setup.ps1 generates one for you).

# =============================================================================
# MQTT (Mosquitto)
# =============================================================================
MQTT_USER=mqtt_user
MQTT_PASSWORD=change_this_password

# =============================================================================
# InfluxDB
# =============================================================================
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=change_this_password
INFLUXDB_ORG=home
INFLUXDB_BUCKET=iot
INFLUXDB_ADMIN_TOKEN=change_this_to_a_secure_token
# Read-only token for Grafana. Create after first start:
#   docker exec iot-influxdb influx auth create --org home --read-bucket <iot bucket id> --description grafana
INFLUXDB_READ_TOKEN=change_this_to_a_read_only_token
# Write-only token for Telegraf (same command with --write-bucket)
INFLUXDB_TELEGRAF_TOKEN=change_this_to_a_write_only_token

# =============================================================================
# Grafana
# =============================================================================
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change_this_password
