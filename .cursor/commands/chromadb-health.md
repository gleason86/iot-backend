# ChromaDB Health

Check ChromaDB heartbeat endpoint.

## Usage
Use to confirm vector store service is up.

## Command
```powershell
cd C:\Users\david\Repos\iot-backend; curl http://localhost:8000/api/v1/heartbeat
```

## Notes
- Ensure container is running; otherwise request fails.
- Adjust host/port if configured differently.

